<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Loan.LoanMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Loan" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Width="750" Height="585" >
    <StackPanel>
        <StackPanel Margin="5" Orientation="Horizontal">
            <StackPanel Margin="5">
                <StackPanel >
                    <TextBox Text="{Binding AccountCode}" Margin="5" materialDesign:HintAssist.Hint="Account Code" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                    <TextBox IsReadOnly="True" Text="{Binding LoanApplication.PersonalData.DisplayName}" Margin="5" materialDesign:HintAssist.Hint="Borrower" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                </StackPanel>
                <StackPanel >
                    <GroupBox Header="Schedule of Payment">
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Width="150" Margin="5">
                                <RadioButton x:Name="dailyRB" Content="Daily" />
                                <RadioButton x:Name="semiMonthlyRB" Content="Semi monthly" IsChecked="True" />
                                <RadioButton x:Name="monthlyRB" Content="Monthly" />

                            </StackPanel>
                            <StackPanel x:Name="SemiMonthly" IsEnabled="{Binding dailyRB.IsChecked}" >
                                <DatePicker x:Name="firstSchedDP" Margin="5 10 5 5" materialDesign:HintAssist.Hint="First Schedule" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                                <DatePicker IsEnabled="{Binding monthlyRB.IsChecked}" x:Name="secondSchedDP" Margin="5 10 5 5" materialDesign:HintAssist.Hint="Second Schedule" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <!--<TextBox IsReadOnly="True" Text="{Binding ReleaseDate}" Margin="5" materialDesign:HintAssist.Hint="Date Realeased" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />-->
                    <DatePicker x:Name="datereleaseDP" SelectedDate="{Binding ReleaseDate}" Margin="5 16 5 5" materialDesign:HintAssist.Hint="Date Realeased" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                    <DatePicker x:Name="firstdueDP" SelectedDate="{Binding FirstDueDate}" Margin="5 16 5 5" materialDesign:HintAssist.Hint="First Due Date" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="MaturityTB" IsReadOnly="True" Text="{Binding MaturityDate}" Margin="5" materialDesign:HintAssist.Hint="Maturity" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                    <TextBox x:Name="AmortizationTB" IsReadOnly="True" Text="{Binding Amortization}" Margin="5" materialDesign:HintAssist.Hint="Amortization" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox IsReadOnly="True" Text="{Binding LoanApplication.CreditTerm.DisplayName}" Margin="5" materialDesign:HintAssist.Hint="Terms of Credit" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                    <TextBox IsReadOnly="True" Text="{Binding InterestRate}" Margin="5" materialDesign:HintAssist.Hint="Interest Rate (Per Month)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox IsReadOnly="True" Text="{Binding Principal}" Margin="5" materialDesign:HintAssist.Hint="Principal" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                    <TextBox IsReadOnly="True" Text="{Binding Interest}" Margin="5" materialDesign:HintAssist.Hint="Interest" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBox IsReadOnly="True" Text="{Binding CashProceeds}" Margin="5" materialDesign:HintAssist.Hint="Cash Proceeds" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                    <TextBox IsReadOnly="True" Text="{Binding Miscellaneous}" Margin="5" materialDesign:HintAssist.Hint="Miscellaneous" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                </StackPanel>
            </StackPanel>
            <StackPanel Margin="5">
                <GroupBox Header="Generated Shedules" Width="295">
                    <DataGrid x:Name="ScheduleDG" ScrollViewer.CanContentScroll="true" Height="430" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                        <DataGrid.Columns>
                            <!--<DataGridTextColumn Header="Schedules" />-->
                            <DataGridTemplateColumn Header="Schedules">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <DatePicker SelectedDate="{Binding Schedule}" Margin="5" ></DatePicker>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>
        </StackPanel>
        
        <StackPanel Background="#FFEEE3E3">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="CancelBtn" Content="cancel" Margin="5" Width="150" />
                <Button x:Name="SaveBTn" Content="Save" Margin="5" Width="150" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
