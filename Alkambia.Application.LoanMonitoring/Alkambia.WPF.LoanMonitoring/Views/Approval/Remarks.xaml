<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Approval.Remarks"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" Title="Remarks" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="310" Width="350">
    <StackPanel>
        <TextBox Text="{Binding Description}" Margin="5" VerticalContentAlignment="Stretch" Height="229"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="cancelbtn" Margin="5" Width="100" Content="Cancel" HorizontalContentAlignment="Center" HorizontalAlignment="Right"></Button>
            <Button x:Name="savebtn" Margin="5" Width="100" Content="Save" HorizontalContentAlignment="Center" HorizontalAlignment="Right"></Button>
        </StackPanel>
        
    </StackPanel>
</controls:MetroWindow>
