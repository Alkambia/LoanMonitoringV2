<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Approval.ApprovalForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" Title="Loan Approvals" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="770">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3" Orientation="Horizontal">
            <StackPanel>
                <TextBox x:Name="searchTB" Margin="5"  Width="305" materialDesign:HintAssist.Hint="Display Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" HorizontalContentAlignment="Left" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="search_buton" Content="search" Margin="5" Width="100"  />
                <Button x:Name="approve_buton" Content="approve" Margin="5" Width="100"  />
                <Button x:Name="decline_button" Content="decline" Margin="5" Width="100"  />
                <Button x:Name="profile_button" Content="profile" Margin="5" Width="100" />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="approvalDG" Margin="5" Height="360" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False" AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=PersonalData.DisplayName}" Header="Applicant" />
                    <DataGridTextColumn Binding="{Binding Path=LoanAmount}" Header="Amount of Loan" />
                    <DataGridTextColumn Binding="{Binding Path=CreditTerm.Term}" Header="Term (in months)" />
                    <DataGridTextColumn Binding="{Binding Path=LoanPurpose}" Header="Purpose" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
