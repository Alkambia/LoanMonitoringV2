<UserControl x:Class="Alkambia.WPF.LoanMonitoring.Views.QuickLinks.QuickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             >
    <DockPanel>
        <StackPanel >
            <Border Margin="5" CornerRadius="5"
                BorderBrush="Gray"
                BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="search_tb" Width="550" Margin="3"  materialDesign:HintAssist.Hint="Search by Name or Voucher Number" Style="{StaticResource MaterialDesignFloatingHintTextBox}" VerticalContentAlignment="Stretch" />
                    <Button x:Name="search_btn" Margin="3" Style="{DynamicResource MetroCircleButtonStyle}" HorizontalAlignment="Right" Height="45" Width="45" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                        <Rectangle Width="20" Height="20" Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                            <Rectangle.OpacityMask>
                                <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_magnify}" />
                            </Rectangle.OpacityMask>
                        </Rectangle>
                    </Button>
                </StackPanel>
            </Border>
            <Border Margin="5" CornerRadius="6"
                BorderBrush="Gray"
                BorderThickness="1">
                <StackPanel Margin="3">
                    <DataGrid x:Name="SearchDG" Height="480" IsReadOnly="True" CanUserAddRows="False" ColumnWidth="*"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" >
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Path=AccountCode}"  Header="Account Number" Width="auto" />
                            <DataGridTextColumn Binding="{Binding Path=LoanApplication.PersonalData.FirstName}" Header="First Name" Width="auto"  />
                            <DataGridTextColumn Binding="{Binding Path=LoanApplication.PersonalData.MiddleName}" Header="Middle Name" Width="auto"  />
                            <DataGridTextColumn Binding="{Binding Path=LoanApplication.PersonalData.LastName}" Header="Last Name" Width="auto"  />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>

            </Border>
        </StackPanel>
        <StackPanel HorizontalAlignment="Right">
            <Border Margin="5" CornerRadius="6"
                BorderBrush="Gray"
                BorderThickness="1">
                <StackPanel>
                    <Button x:Name="application_btn" Margin="3" Content="Application" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Top"></Button>
                    <Button x:Name="payment_btn" Margin="3" Content="Payment" ></Button>
                    <Button x:Name="payment_sum_btn" Margin="3" Content="Payment Summary" ></Button>
                </StackPanel>
                
            </Border>
            
        </StackPanel>
    </DockPanel>
    
    
</UserControl>
