<controls:MetroWindow  x:Class="Alkambia.WPF.LoanMonitoring.Views.Releasing.Releasing"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Releasing" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="770">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="release_buton" Content="release" Margin="5" Width="135"  />
                <Button x:Name="reapprove_button" Content="re-approve" Margin="5" Width="135"  />
                <Button x:Name="profile_button" Content="profile" Margin="5" Width="135" />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="RealesingDG" ScrollViewer.CanContentScroll="true" Height="370" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.PersonalData.DisplayName}" Header="Applicant" />
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.LoanAmount}" Header="Amount of Loan" />
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.CreditTerm.DisplayName}" Header="Term" />
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.LoanPurpose}" Header="Purpose" />
                    <DataGridTextColumn Binding="{Binding Path=ApprovalDate}" Header="Date" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
