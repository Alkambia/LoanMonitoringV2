<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Releasing.Released"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" Title="Released Loans" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="770">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3" Orientation="Horizontal">
            <StackPanel>
                <TextBox x:Name="searchTB" Margin="5"  Width="365" materialDesign:HintAssist.Hint="Display Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" HorizontalContentAlignment="Left" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="search_buton" Content="search" Margin="5" Width="100"  />
                <Button x:Name="docu_button" Content="document" Margin="5" Width="100"  />
                <Button x:Name="unrelease_button" Content="unrelease" Margin="5" Width="100" />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="ItemsDG" Margin="5" Height="360" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False" AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=AccountCode}" Header="Account Code" />
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.PersonalData.DisplayName}" Header="Borrower" />
                    <DataGridTextColumn Binding="{Binding Path=Principal}" Header="Principal" />
                    <DataGridTextColumn Binding="{Binding Path=LoanApplication.CreditTerm.Term}" Header="Term (in months)" />
                    <DataGridTextColumn Binding="{Binding Path=ReleaseDate}" Header="Release Date" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
