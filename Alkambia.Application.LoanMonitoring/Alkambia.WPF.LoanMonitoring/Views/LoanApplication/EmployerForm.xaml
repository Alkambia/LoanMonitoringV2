<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.LoanApplication.EmployerForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
             mc:Ignorable="d" Title="Employer" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Width="446.635" Height="465" >
    <StackPanel>
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding EmployerName}" Margin="5" materialDesign:HintAssist.Hint="Employer" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                <TextBox Text="{Binding Position}" Margin="5" materialDesign:HintAssist.Hint="Position" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
            </StackPanel>
            <StackPanel >
                <TextBox Text="{Binding Address}" Margin="5" Height="100" TextWrapping="Wrap" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto"  materialDesign:HintAssist.Hint="Address"  Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding LenghtOfService}" Margin="5" materialDesign:HintAssist.Hint="Length of Service" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                <TextBox Text="{Binding BusinessNature}" Margin="5" materialDesign:HintAssist.Hint="Nature of Business" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="EmploymentStatusCB" SelectedItem="{Binding Status}" Margin="5 16 5 5" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="Employment Status" Width="200" materialDesign:ComboBoxAssist.ClassicMode="{Binding ElementName=ClassicModeCheckBox, Path=IsChecked}" >
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBox Text="{Binding MonthlySalary}" Margin="5" materialDesign:HintAssist.Hint="Mothly Salary" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Telephone}" Margin="5" materialDesign:HintAssist.Hint="Telephone" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                <TextBox Text="{Binding Superior}" Margin="5" materialDesign:HintAssist.Hint="Immediate Supperrior" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <DatePicker SelectedDate="{Binding EmployedFrom}" Margin="5 16 5 5" materialDesign:HintAssist.Hint="Date Employed From" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                <DatePicker SelectedDate="{Binding EmployedTo}" Margin="5 16 5 5" materialDesign:HintAssist.Hint="Date Employed To" Width="200" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
            </StackPanel>
        </StackPanel>
        <StackPanel Background="#FFEEE3E3">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="EmployerCancelBtn" Content="cancel" Margin="5" Width="150" />
                <Button x:Name="EmployerAddBtn" Content="add" Margin="5" Width="150" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
