<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.LoanApplication.SourceOfIncomeForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        Title="Other Source of Income" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Width="446.635" Height="auto" >
    <StackPanel>
        <StackPanel Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Nature}" Margin="5" materialDesign:HintAssist.Hint="Nature" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                <TextBox Text="{Binding Income}" Margin="5" materialDesign:HintAssist.Hint="Income" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
            </StackPanel>
            <StackPanel >
                <ComboBox x:Name="IncomeType"
                          SelectedValue="{Binding Type}"
                          SelectedValuePath="Content"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          Margin="5"
                          materialDesign:HintAssist.Hint="Type"
                          materialDesign:ComboBoxAssist.ClassicMode="{Binding ElementName=ClassicModeCheckBox, Path=IsChecked}">
                    <ComboBoxItem Content="Daily" />
                    <ComboBoxItem Content="Monthly" />
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Background="#FFEEE3E3">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="CancelBtn" Content="cancel" Margin="5" Width="150" />
                <Button x:Name="AddBTn" Content="add" Margin="5" Width="150" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
