<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.LoanApplication.PropertyForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Personal &amp; Real Property Owned" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Width="446.635" Height="330">
    <StackPanel>
        <StackPanel Margin="10">
            <StackPanel>
                <!--<TextBox Margin="5" materialDesign:HintAssist.Hint="Kind" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />-->
                <ComboBox x:Name="KindCB" SelectedItem="{Binding Kind}"  Margin="5 16 5 5" Style="{StaticResource MaterialDesignFloatingHintComboBox}"  materialDesign:HintAssist.Hint="Kind" materialDesign:ComboBoxAssist.ClassicMode="{Binding ElementName=ClassicModeCheckBox, Path=IsChecked}" >
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
            <StackPanel >
                <TextBox Text="{Binding Location}" Margin="5" Height="100" TextWrapping="Wrap" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto"  materialDesign:HintAssist.Hint="Location"  Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding Value}" Margin="5" materialDesign:HintAssist.Hint="Value" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />
                <!--<TextBox Margin="5" materialDesign:HintAssist.Hint="Encumbrances" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="200" />-->
                <ComboBox Margin="5 16 5 5" SelectedIndex="{Binding Encumbrances}" Style="{StaticResource MaterialDesignFloatingHintComboBox}" materialDesign:HintAssist.Hint="Encumbrances" materialDesign:ComboBoxAssist.ClassicMode="{Binding ElementName=ClassicModeCheckBox, Path=IsChecked}" Width="200" >
                    <ComboBoxItem>No</ComboBoxItem>
                    <ComboBoxItem>Yes</ComboBoxItem>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Background="#FFEEE3E3">
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="CancelBtn" Content="cancel" Margin="5" Width="150" />
                <Button x:Name="AddBtn" Content="add" Margin="5" Width="150" />
            </StackPanel>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>