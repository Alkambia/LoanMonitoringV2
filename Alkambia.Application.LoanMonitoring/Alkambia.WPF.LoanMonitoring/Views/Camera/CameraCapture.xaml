<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Camera.CameraCapture"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Capture" ResizeMode="NoResize" Height="460" Width="660">
    <StackPanel Orientation="Horizontal">
        <StackPanel Margin="10">
            <Border  Grid.Column="1" Grid.Row="1" BorderThickness="3" CornerRadius="3">
                <Border.BorderBrush>
                    <RadialGradientBrush>
                        <GradientStop Color="Black" Offset="0.047"/>
                        <GradientStop Color="#FF00907A" Offset="1"/>
                    </RadialGradientBrush>
                </Border.BorderBrush>
                <Image x:Name="CameraImage" Width="400" Height="400" Stretch="Fill"   />
            </Border>
        </StackPanel>
        
        <StackPanel Margin="10">
            <StackPanel>
                <Border  Grid.Column="1" Grid.Row="1" BorderThickness="3" CornerRadius="3">
                    <Border.BorderBrush>
                        <RadialGradientBrush>
                            <GradientStop Color="Black" Offset="0.047"/>
                            <GradientStop Color="#FF00907A" Offset="1"/>
                        </RadialGradientBrush>
                    </Border.BorderBrush>
                    <Image x:Name="CameraCaptureImg" Width="200" Height="200" Stretch="Fill"  />
                </Border>
            </StackPanel>
            <StackPanel>
                <Button x:Name="capture_btn" Margin="0,7,0,2" Content="Capture"></Button>
                <Button x:Name="upload_btn" Margin="0,7,0,2" Content="Upload Photo"></Button>
                <Button x:Name="video_format_btn" Margin="0,7,0,2" Content="Video Format"></Button>
                <Button x:Name="video_src_btn" Margin="0,7,0,2" Content="Video Source"></Button>
                <Button x:Name="close_btn" Margin="0,7,0,2" Content="Close"></Button>
            </StackPanel>
            
            
        </StackPanel>
    </StackPanel>
    
</controls:MetroWindow>
