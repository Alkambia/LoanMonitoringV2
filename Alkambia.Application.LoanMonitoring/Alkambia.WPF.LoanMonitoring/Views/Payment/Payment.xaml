<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Payment.Payment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
             mc:Ignorable="d" Title="Loan Payments" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="840">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3" Orientation="Horizontal">
            <StackPanel HorizontalAlignment="Left">
                <TextBox x:Name="accountCodeTB" Margin="5" materialDesign:HintAssist.Hint="Account Code" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="300" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="search_buton" Content="search" Margin="5" Width="120"  />
                <Button x:Name="payment_button" Content="new payment" Margin="5" Width="120" />
                <Button x:Name="edit_button" Content="edit payment" Margin="5" Width="120" />
                <Button x:Name="del_button" Content="delete" Margin="5" Width="120" />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="PaymentsDG" Height="370" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=Date}" Header="Date" />
                    <DataGridTextColumn Binding="{Binding Path=ORNumber}" Header="OR Number" />
                    <DataGridTextColumn Binding="{Binding Path=Principal}" Header="Principal" />
                    <DataGridTextColumn Binding="{Binding Path=Interest}" Header="Interest" />
                    <DataGridTextColumn Binding="{Binding Path=Amount}" Header="Amount" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
