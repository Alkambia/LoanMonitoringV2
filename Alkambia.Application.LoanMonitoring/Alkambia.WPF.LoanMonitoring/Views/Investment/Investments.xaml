<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Investment.Investments"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Investments" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="700">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3" Orientation="Horizontal">
            <StackPanel HorizontalAlignment="Left">
                <TextBox x:Name="diplaynameTB" Margin="5" materialDesign:HintAssist.Hint="Display Name" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="240" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="search_buton" Content="search" Margin="5" Width="100"  />
                <Button x:Name="investment_button" Content="add" Margin="5" Width="100" />
                <Button x:Name="inv_edit_btn" Content="edit" Margin="5" Width="100" />
                <Button x:Name="investment_del" Content="delete" Margin="5" Width="100" />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="inverstmentDG" Height="370" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=CreatedDate}" Header="Date" />
                    <DataGridTextColumn Binding="{Binding Path=Name}" Header="Name" />
                    <DataGridTextColumn Binding="{Binding Path=DisplayName}" Header="Display Name" />
                    <DataGridTextColumn Binding="{Binding Path=Capital}" Header="Capital" />
                    <DataGridTextColumn Binding="{Binding Path=IsApproved}" Header="BIR Approved" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
