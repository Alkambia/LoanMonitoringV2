<controls:MetroWindow x:Class="Alkambia.WPF.LoanMonitoring.Views.Expense.Expense"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Alkambia.WPF.LoanMonitoring.Views.Expense"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        mc:Ignorable="d"
        Title="Expense" ResizeMode="NoResize" 
                      WindowStartupLocation="CenterScreen" Height="457" Width="725">
    <StackPanel>
        <StackPanel Background="#FFEEE3E3" Orientation="Horizontal">
            <StackPanel HorizontalAlignment="Left">
                <DatePicker x:Name="ExpenseDateDP" Margin="5 16 5 5" materialDesign:HintAssist.Hint="Date of Expense" Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                <Button x:Name="search_buton" Content="search" Margin="5" Width="115"  />
                <Button x:Name="expense_button" Content="new expense" Margin="5" Width="115"  />
                <Button x:Name="edit_button" Content="edit expense" Margin="5" Width="115"  />
                <Button x:Name="delete_button" Content="delete" Margin="5" Width="115"  />
            </StackPanel>
        </StackPanel>

        <StackPanel>
            <DataGrid x:Name="ExpenseDG" Height="370" ColumnWidth="*" IsReadOnly="True" CanUserAddRows="False"  AutoGenerateColumns="False" ClipboardCopyMode="IncludeHeader" EnableRowVirtualization="False" >
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=ExpenseDate}" Header="Date" />
                    <DataGridTextColumn Binding="{Binding Path=DisplayName}" Header="Display Name" />
                    <DataGridTextColumn Binding="{Binding Path=Amount}" Header="Amount" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </StackPanel>
</controls:MetroWindow>
